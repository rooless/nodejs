const http = require('http');
const port = 8080;

exports.connect = function(func, file1, file2) {
    http.createServer((req, res) => {
        func(file1,file2,res);
    }).listen(port, () => {
        console.log('Server listening on: http://localhost:%s', port);
    });
}
